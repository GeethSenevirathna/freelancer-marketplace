# Example environment variables for local development
# Replace with your Google OAuth Client ID (Web application)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# When true, frontend will mock the /api/auth/google response so you can test
# Google sign-in UI without a backend. Set to "false" when you have a backend.
VITE_USE_MOCK_AUTH=true
